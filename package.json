{
  "name": "can-logger-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "PC-hosted customer dashboard for the ESP32 CAN logger.",
  "type": "module",
  "scripts": {
    "setup": "node --version",
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
